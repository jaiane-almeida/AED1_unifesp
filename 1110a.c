//exercicios das cartas sendo implementadas com listas, filas ou pilhas. O exercicio pede para que vc retire a primeira carta do topo e coloque a proxima q vier na base e vai seguindo 

#include <stdio.h>

  int main(){
  int n, desc =0, base;
  int pilhas[n]; 
  int carte[0];
  int baixo[0];
  

  printf("quantas cartas voce quer adicionar?");
  scanf("%d",n); 

  if (n >50 ){
    printf("valor invalido");
  }

  printf("Discarded cards:");
  for(int i =0; i < n; i++){
    desc = desc +1;
    carte[desc];
    printf("%d", carte[desc]);
    base = desc +1;
    baixo[base];
  }
 
 printf("Remaining card: "); 
}
